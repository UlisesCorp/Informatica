#include<stdio.h>

#define N 5

int main(void)
{

	int i,p;
	int a[N];
	int m;
	
	printf("ingrese los 5 elementos del arreglo \n");
	
	for(i=0; i<5; i++){
		printf("ingrese el elemento a[%d] \n", i);
		scanf("%d", &a[i]);
	}
	printf("Los elementos del arreglo que contienen la suma acumulada son...\n");
	for (i=0; i<5; i++)
		printf(b[i]
